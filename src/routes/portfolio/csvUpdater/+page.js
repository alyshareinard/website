export function load() {
	return {
		title: 'CSV Cleaner',
		description:
			'A free tool that cleans up CSV files in your browser: format phone numbers and dates, split or combine ' +
			'columns, rename or remove columns, and remove duplicates. Rule-based code, not AI, and your file is never uploaded.'
	};
}
